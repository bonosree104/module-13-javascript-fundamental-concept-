var friendAge = [15, 17, 18, 20];

 var rupaliAge = friendAge[2];

  friendAge[1] = 50;

  var position = friendAge.indexOf(181);
  friendAge.push(44);
  friendAge. push(33);
  friendAge.pop();
console.log(friendAge.length);